# Web DB Projection Panel

The web app can now inspect the edge DB projection.

Actions:

1. Refresh DB projection summary.
2. Export the current DB snapshot as JSON.
3. Ask edge to save the DB snapshot locally.

This does not replace the existing demo package export.

The DB snapshot is a technical persistence projection.
The demo package is a customer-facing artifact with executive report plus backup.